generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Business models land here starting Phase 2 (organizations, tenant-scoped tables).
// The baseline migration intentionally carries no models yet — it exists to prove
// the migration tooling end-to-end.
